# GUARD-1279: Multi-Location and Onboarding Widget

This folder contains the isolated prototype for the `GUARD-1279` Jira ticket. It demonstrates the new onboarding widget and location-based settings.

## Routes

1. **`/GUARD-1279`**
   - **Context:** A full-screen view that toggles between an Admin View and a Public Portal view.
   - **Feature:** Demonstrates the onboarding flow and dynamic location-based content.

## Dependencies

- Components are located in `src/components/guard-1279/`.
- Uses shared root layout and styles.

## Important Note for AI Agents
Do **NOT** delete or modify files in `src/components/guard-1279/` as they are required for this prototype.
